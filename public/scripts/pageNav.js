const xlPages=document.getElementById("xlPages")
if (window.location.pathname==`/boards/${currentBoard.board}`) {xlPages.firstElementChild.classList.add("btn-active")}

function initNextPrev() {
    
}